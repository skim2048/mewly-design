# Mewly v3 화면 재현 — 인계 요약

## 프로젝트
`skim2048/mewly`(Vue 3 · Vite · Capacitor, Android) 프론트엔드와 `skim2048/babycat`(FastAPI, Jetson) 백엔드의 화면을 v3 어법으로 재현하는 작업이다. babycat은 참조 전용이고 변경 추적 기준은 mewly다. 재현 근거는 스크린샷이 아니라 `.vue`·`config/*.json`·`i18n/messages.js` 판독이다. 리포지토리 정보는 프로젝트 루트 `github.md`에 있다.

## 최상위 규칙
`CLAUDE.md`와 `01_Component_Catalog.dc.html`이 규칙이다. 카탈로그가 사양이며, 화면과 다르면 화면을 카탈로그에 맞춘다. 카탈로그 수정은 사양 변경이므로 사용자 승인 없이 하지 않는다. 카탈로그에 값이 없어 판단이 필요하면 임의로 정하지 않고 확정을 요청하고, 확정된 값은 그 자리에서 카탈로그에 문장으로 남긴다.

## 파일 현황
- `01_Component_Catalog.dc.html` — 사양 문서
- `02_Login`, `03_Home_Tab`(기기 시트 조명·온도·마이크·PTZ, 회전 전체화면 포함), `04_Schedule_Tab`, `05_Analysis_Tab`, `06_Settings_Profile`, `07_Settings_Camera`, `08_Settings_Sheets`, `09_Notifications_Overlay` — 완료. 각 시트는 라이트·다크를 나란히 둔다.
- 구 버전 재현은 `Structure_Map` 하나만 남았다(v3 대응 시트 없음). 나머지 6개는 삭제했다.
- 2026-09-07: 파일 이름을 영어·밑줄 형식 `NN_Screen_Name.dc.html`로 바꾸고 「Mewly」「V3」 접두어를 뺐다. 코드 주석은 전부 영어다. 이 문서의 아래 문단이 말하는 `V3-N`은 같은 번호의 `0N_` 파일이다.
- 2026-09-07: 예전 `V3-8 기기 제어 시트`를 `V3-3 홈 탭`에 합치고 삭제했다. 그에 맞춰 `V3-9 카메라 설정`→`V3-8`, `V3-10 설정 시트`→`V3-9`로 번호를 당기었다.
- 2026-09-07(같은 날 뒤): 설정 축(6·7·8)을 붙이기 위해 다시 배정했다. `V3-8 카메라 설정`→`V3-7 설정·카메라`, `V3-9 설정 시트`→`V3-8 설정·시트`, `V3-7 알림 오버레이`→`V3-9`. 이 문서의 아래 문단이 말하는 `V3-10 설정 시트`는 지금의 `V3-8 설정·시트`다. 알림함은 MainView.vue에서 상단 바 종 버튼으로 열리므로 설정 축에 넣지 않았다.
- 자산: NanumSquare·Inter·Patrick Hand, Phosphor 아이콘, `user_profile.svg`, `public/crop-placeholder.png`

## 설정·시트(현 V3-8, 당시 V3-10) — 확정한 것
분석 설정·프롬프트 설정·비밀번호 변경·사진 조정 네 하위 화면으로 완료 상태다.

- 해석되지 않는 `ref` 두 개 제거, 분석 설정 닫기 버튼 동작 보완.
- 휠이 열려 있을 때만 화면 전체를 덮는 취소 레이어(z-index 4, 휠 본체 5)를 두어 휠 밖을 누르면 닫힌다. 「입력하는 층」(프롬프트·비밀번호)에는 두지 않는다.
- 입력 칸 열 개(라이트·다크 각 5) 모두 카탈로그 02 입력 필드 사양이다. placeholder로 라벨을 대신하는 곳은 없다.
- 프롬프트 시트: 경고 카드 안의 복원 버튼을 없애고 하단 조작 줄에 `초기화`(보조) — `저장`(주) 순으로 두었다. 초기화는 프롬프트만 검증본으로 되돌리며, 버튼이 뜨는 조건도 프롬프트만 본다. 키워드는 초기화의 대상도 조건도 아니다.
- 라벨 문안은 `프롬프트`, `키워드`다. 둘 다 여러 줄 칸이다.

**카탈로그 02에 신설한 「여러 줄 칸」 사양** — 라벨은 칸 안에 둔다. 올라간 위치 top 9px(한 줄 칸과 같음), 내려간 위치 top 23px(한 줄 칸은 18px). 입력 영역 패딩 23px 14px 13px, 줄높이 1.6. 높이는 고정하지 않고 최소 높이로 준다(2줄 84px, 4줄 132px). 크기 조절 손잡이 없음. 반경 14, 테두리 없음. `field(k, multi)` 판정을 카탈로그와 설정·시트가 공유한다.

## 사용자가 그대로 두기로 한 것
- 라이트·다크가 값·초점·크롭 상태를 공유한다. 다른 시트도 같은 방식이다.
- 키워드의 줄바꿈은 구분자로 처리하지 않고 값에 남는다.

## 다음 작업 — 2026-09-07 기준
위 「다음 작업」 네 항목은 모두 끝났다. 구 버전 재현 6개 삭제, 기기 제어 시트와 PTZ는 V3-3에 합쳐짐, 카메라 설정은 V3-7 설정·카메라, 텍스트 레이블 일괄 대조는 완료.

텍스트 레이블 대조에서 확정된 것:
- 용어: 원본 i18n과 다르게 확정한 낱말은 카탈로그 첫머리 「용어」 표에 모인다. 현재 「완료」(sched.done 마침), 「닭지 않음」(ana.acto.sub 비누움). 「문안」이라는 말은 쓰지 않고 「텍스트 레이블」 또는 「레이블」로 부른다.
- 원본에 없으나 사양으로 확정: V3-4 완료·되돌리기 조작, V3-3 냉난방 예약.
- 원본과 다르게 유지: V3-4 카테고리 칩 없음(이름 칸만), V3-6 견종 드롭다운(검색 모드 없음).
- 원본을 따라 추가: V3-3 카메라 꺼짐·로그 없음, V3-5 이벤트 없음·추론 데이터 없음·탭하여 닫기, V3-9(알림) 일정 각주. 상태는 각 시트의 Tweaks로 전환한다.
- 카탈로그 04에 「뱈 상태의 두 등급」(화면 전체 = 세 줄 + 버튼, 칸 하나 = 한 줄)과 「행의 각주 줄」을 문장으로 남김.

남은 일: 없음. 카탈로그 전수 대조는 2026-09-07에 끝냈다(V3-3·V3-5의 한 줄 빈 상태 크기 셋을 0.8125rem으로 맞춤). 저장소는 `skim2048/mewly-design`(master)이며, 커밋·푸시는 사용자가 로컬에서 한다(이 환경에는 git 도구가 없다). `CLAUDE.md`·`skills/`는 저장소에 올리지 않는다(로컬 `.claude/` 아래, `.gitignore` 제외). 이후 세션: 설정 시트 단일 파일 병합, 모바일 설계 규칙 스킬 적용.

## 다음 작업 — 이전 기록(2026-09-04, 모두 완료)
1. **구 버전 재현 정리.** `Mewly 구조 지도`를 뺀 6개는 v3로 대체되어 삭제 대상이다. 구조 지도는 v3 대응 시트가 없어 남길지 판단이 필요하다. 삭제 목록을 먼저 제시하고 승인을 받는다.
2. **V3-8 기기 제어 시트(조명·온도·마이크).** 구 버전 `Mewly 기기 시트.dc.html`이 참고 대상이나 원본 `.vue`·`config/*.json`을 다시 판독한다. 밝기·온도의 연속값 조작 부품(슬라이더)이 카탈로그 02에 없으므로 값 확정 요청이 필요하다.
3. **V3-9 카메라·PTZ.** 구 버전 `Mewly 클립·카메라`와 `Mewly PTZ·설정 시트`에 흩어져 있다. PTZ 방향 조작판도 카탈로그에 없는 부품이다.
4. **문안 일괄 대조.** 전 시트 문안을 `i18n/messages.js`와 한 번에 대조한다. V3-8·V3-9 완료 후에 한다.

## 파일 이름 규칙
`NN_Screen_Name.dc.html` — 두 자리 번호, 영어, 구분자는 `_`만 쓴다(공백·가운뎃점 없음). 순서는 Component_Catalog(01) → Login(02) → Home_Tab(03) → Schedule_Tab(04) → Analysis_Tab(05) → 설정 축 Settings_Profile(06) · Settings_Camera(07) · Settings_Sheets(08) → Notifications_Overlay(09)이며 새 화면은 뒤 번호를 이어 붙인다. 접미사를 새로 만들지 않는다.

## 언어 규칙(2026-09-07 확정)
영어: 파일 이름, `.js`·`.jsx`, 로직 클래스 코드, 코드 주석, 커밋 메시지. 한국어 유지: 화면 안 텍스트 레이블(원본 i18n 재현), 카탈로그 사양 문장, `github.md`·이 문서.
